{
  "name": "hash-wallet-prices",
  "version": "0.1.0",
  "private": true,
  "description": "Price aggregator for Hash Wallet — Cloudflare Worker that fetches USD prices from Kraken (majors) and NonKYC (Wownero) and serves them to the app",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.90.0"
  }
}
